# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Local env files (never commit secrets)
.env
.env.local
.env.*.local

# Local file storage (runtime state, not source — plan.md §10)
/uploads/

# Local MongoDB binaries/data, if installed portably inside the project
# folder (plan.md §10) — never committed, machine-specific runtime state.
/mongodb/
/mongodb-data/

# Build/debug
*.tsbuildinfo
npm-debug.log*

# OS/editor
.DS_Store
